# Copy to .env (untracked). Read by CMake at configure time.
# Encrypted provider key baked into the plugin as the default providerKey.
SCORBIT_PROVIDER_KEY=
# Scorbit SDK key secret, needed for the SDK to decrypt provider keys at runtime.
SCORBIT_SDK_ENCRYPT_SECRET=
